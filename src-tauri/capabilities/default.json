{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主視窗需要的最小權限：只有核心的 invoke 與事件，沒有檔案系統或 shell 的前端存取",
  "windows": ["main"],
  "permissions": ["core:default"]
}
